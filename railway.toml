[build]
builder = "RAILPACK"
buildCommand = "mvn -B clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar target/smart-forest-irrigation.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
