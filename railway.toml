[build]
builder = "RAILPACK"
buildCommand = "mvn -B clean package -DskipTests"

[deploy]
startCommand = "sh -c 'exec java -Dserver.port=$PORT -jar target/*.jar'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
